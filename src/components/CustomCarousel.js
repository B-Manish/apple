import React from "react";
import { Box } from "@mui/material";

const CustomCarousel = () => {
  return (
    <Box sx={{ background: "black", height: "100vh", color: "white" }}>
      CustomCarousel
    </Box>
  );
};

export default CustomCarousel;
